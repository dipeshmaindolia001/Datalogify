---
id: "sql-078"
topic: "sql"
title: "Percentage Contribution of Product Sales to Total Monthly Sales"
level: "advanced"
type: "window-functions"
tags: ["window-functions","pct-contribution","group-by"]
order: 78
question: |
  Write a SQL query to calculate the percentage contribution of each product's sales to total monthly sales revenue.
sampleData: |
  `sales` table: `product_id`, `sale_date`, `amount`
answer: |
  ```sql
  WITH MonthlySales AS (
      SELECT product_id, 
             DATE_TRUNC('month', sale_date) AS month, 
             SUM(amount) AS product_sales
      FROM sales
      GROUP BY product_id, DATE_TRUNC('month', sale_date)
  ),
  TotalMonthlySales AS (
      SELECT month, SUM(product_sales) AS total_sales
      FROM MonthlySales
      GROUP BY month
  )
  SELECT ms.product_id, ms.month, ms.product_sales,
         (ms.product_sales * 100.0) / tms.total_sales AS pct_contribution
  FROM MonthlySales ms
  JOIN TotalMonthlySales tms ON ms.month = tms.month;
  ```
explanation: |
  - Aggregates sales per product-month and total sales per month in CTEs, computing exact percentage share.
---
